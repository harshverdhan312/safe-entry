<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/stylesheets/styles.css" rel="stylesheet">
    <title>User Dashboard</title>
</head>
<body class="bg-gray-100 font-sans">
    <div class="container mx-auto p-4 sm:p-6 lg:p-8">
        <div class="flex justify-between items-center mb-6">
            <h1 class="text-3xl font-bold text-gray-800">Your Dashboard</h1>
            <% if (userId) { %>
                <div class="text-right">
                    <p class="text-gray-600">Welcome, <%= user ? user.name : 'User' %>!</p>
                    <p class="text-sm text-gray-500 font-mono">ID: <%= userId %></p>
                </div>
            <% } %>
        </div>

        <!-- Prompt to complete profile -->
        <% if (user && (!user.contactNumber || !user.place)) { %>
            <div class="bg-yellow-100 border-l-4 border-yellow-500 text-yellow-700 p-4 mb-6" role="alert">
                <p class="font-bold">Complete Your Profile</p>
                <p>Please update your profile with your contact number and place to continue.</p>
            </div>
        <% } %>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <!-- Create QR Section -->
            <section id="create-qr" class="bg-white p-6 rounded-lg shadow-md">
                <h2 class="text-xl font-semibold mb-4 text-gray-700">Create a New QR Code</h2>
                <form action="/api/v1/regular/create-qr" method="POST">
                    <!-- Assuming userId is handled on the backend via session/JWT -->
                    <button type="submit" class="w-full bg-blue-500 text-white py-2 px-4 rounded-md hover:bg-blue-600">Generate QR Code</button>
                </form>
            </section>

            <!-- Visitor Count Section -->
            <section id="visitor-count" class="bg-white p-6 rounded-lg shadow-md text-center">
                <h2 class="text-xl font-semibold mb-2 text-gray-700">Total Visitors Scanned</h2>
                <p class="text-5xl font-bold text-blue-600">
                    <%= user && user.numberOfVisitors ? user.numberOfVisitors : 0 %>
                </p>
            </section>


            <!-- Download QR Section -->
            <section id="download-qr" class="bg-white p-6 rounded-lg shadow-md">
                <h2 class="text-xl font-semibold mb-4 text-gray-700">Your QR Code</h2>
                <p class="text-gray-600">Your new QR code will be displayed here for download.</p>
                <!-- Example: <img src="path/to/qr-code.png" alt="Your QR Code" class="mt-4 border rounded-md"> -->
            </section>

            <!-- Update Profile Section -->
            <section id="update-profile" class="bg-white p-6 rounded-lg shadow-md">
                <h2 class="text-xl font-semibold mb-4 text-gray-700">Update Your Profile</h2>
                <form action="/api/v1/users/update-details" method="POST" class="space-y-4">
                    <% if (userId) { %>
                        <input type="hidden" name="userId" value="<%= userId %>">
                        <input type="hidden" name="userType" value="regular">
                    <% } %>
                    <input type="text" name="name" placeholder="New Name" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                    <input type="tel" name="contactNumber" placeholder="Contact Number" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                    <input type="text" name="place" placeholder="Place / Address" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                    <button type="submit" class="w-full bg-green-500 text-white py-2 px-4 rounded-md hover:bg-green-600">Update Profile</button>
                </form>
            </section>
        </div>
    </div>
</body>
</html>